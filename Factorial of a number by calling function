#include<stdio.h>
#include<conio.h>
void main()
{
int n;
printf(“Enter the number :”);
scanf (“%d", &n);
printf(“factorial is:”);
factorial(n);
}

int factorial(int x)
{
int fact=1,i;
for(i=2;i<=x;i++)
{
fact=fact*i;
}
return(fact);
}
